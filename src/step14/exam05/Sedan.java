package step14.exam05;

public class Sedan extends Car { // Car 클래스 상속받음 -> 메소드도 상속받음 -> 상속받은 메소드 사용 가능
                                //(Sedan은 인스턴스 생성 가능)
  boolean isAutomatic;
  boolean isSunroof;
}
